# eye-disease-classification
Eye Disease Classification using Deep Learning  This project uses a Convolutional Neural Network (CNN) to classify eye diseases such as Cataract, Conjunctivitis, Uveitis, Eyelid, and Normal using medical image datasets. The model is built using TensorFlow and trained on labeled eye images.
